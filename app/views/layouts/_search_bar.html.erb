<%= form_with url: searches_path do |f| %>
  <%= f.text_field :search, placeholder: "Search Movies" %>
  <%= f.button(type: "submit", class: "btn btn-transparent text-white" ) do %>
    <i class="fas fa-search"></i>
  <% end %>
<% end %>