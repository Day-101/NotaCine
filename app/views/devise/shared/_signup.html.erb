<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= button_to "Sign up", new_registration_path(resource_name), :method => "get", :class => 'btn btn-outline-secondary btn-block' %><br />
<% end %>
